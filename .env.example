# n8n API Configuration
# Local n8n instance URL (default: http://localhost:5678)
N8N_API_URL=http://localhost:5678

# n8n Login Credentials
# These are your n8n instance login credentials (email/password)
# Used for session-based authentication with n8n's internal API
N8N_EMAIL=your_email@example.com
N8N_PASSWORD=your_password_here
